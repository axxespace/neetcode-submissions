class Solution {
    /**
     * @param {number[]} nums
     * @param {number} target
     * @return {number[]}
     */
    twoSum(nums, target) {
        for (const [index, num] of nums.entries()) {
            console.log(num) 
        }
    }
}
