class Solution {
    /**
     * @param {string[]} strs
     * @return {string[][]}
     */
    
    groupAnagrams(strs) {
        console.log('aaa')
        const list = new Map();
        for (aaa in strs) {
            console.log(aaa)
        }
    }
}
