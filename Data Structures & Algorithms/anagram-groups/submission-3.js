class Solution {
    /**
     * @param {string[]} strs
     * @return {string[][]}
     */
    
    groupAnagrams(strs) {
        console.log('aaa')
        const list = new Map();
        for (strs of strs) {
            console.log(strs)
        }
    }
}
