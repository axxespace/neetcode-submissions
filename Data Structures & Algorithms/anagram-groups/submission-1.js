class Solution {
    /**
     * @param {string[]} strs
     * @return {string[][]}
     */
    
    groupAnagrams(strs) {
        console.log('aaa')
        const list = new Map();
        for (const str in strs) {
            console.log(str)
        }
    }
}
