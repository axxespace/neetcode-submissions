class Solution {
    /**
     * @param {string[]} strs
     * @return {string[][]}
     */
    
    groupAnagrams(strs) {
        const list = new Map();
        for (const strs of strs) {
            console.log(strs)
        }
    }
}
